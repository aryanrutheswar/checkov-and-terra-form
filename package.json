{
  "name": "terraform-checkov-security-suite",
  "version": "1.0.0",
  "private": true,
  "description": "Terraform + Checkov Security Suite",
  "scripts": {
    "dev": "npm --prefix website run dev",
    "build": "npm --prefix website run build",
    "backend": "python -m uvicorn backend.main:app --reload --port 8000",
    "start": "npm run dev"
  }
}
